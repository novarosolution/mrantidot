# Set BEFORE running: eas build -p android --profile preview
# Standalone APK cannot use localhost or LAN IP unless the phone is on your Wi-Fi.
#
# Options:
#   1) Deploy API to Railway/Render/Fly.io → https://your-app.example.com
#   2) ngrok: ngrok http 4000 → https://xxxx.ngrok-free.app
#
EXPO_PUBLIC_API_URL=https://your-public-api.example.com
