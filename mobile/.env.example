# Production / shared API — also set in deploy.config.json at repo root (EAS builds).
# Dev: Metro auto-detects LAN by default. To pin a remote server, set URL + FORCE:
EXPO_PUBLIC_API_URL=http://13.60.23.65:4001
EXPO_PUBLIC_API_URL_FORCE=true
# Local only examples:
# EXPO_PUBLIC_API_URL=http://127.0.0.1:4001
# EXPO_PUBLIC_API_URL=http://192.168.1.13:4001
