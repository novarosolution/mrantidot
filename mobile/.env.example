# In Expo dev, the API URL is auto-detected from the Metro host (see mobile/lib/config.ts).
# You only need to set this for production/standalone builds (EAS).
# EXPO_PUBLIC_API_URL=https://your-public-api.example.com
