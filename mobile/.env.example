# Production API URL — see deploy.config.json at repo root (used by EAS builds).
# Dev uses auto-detected Metro host (lib/config.ts); optional LAN fallback:
# EXPO_PUBLIC_API_URL=http://192.168.1.4:4000
